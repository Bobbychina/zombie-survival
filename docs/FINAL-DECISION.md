# FINAL-DECISION · 《丧尸末日生存 v2》v2.1 迭代范围（评审终稿）

- 评审房间：`room-mtxaucnk-1`（3 轮 / 25 条消息 / 5 名成员）
- 评审程序说明：`LLMSummary` 草案生成失败 → 自动降级多数投票 → 表态 1/5 未达法定人数，系统状态记为"附修订通过 · 待确认"。逐轮摘要中各成员红线状态在第 2、3 轮均已标为**满足**，故本文件按"附修订通过"固化范围；未闭合项由主 agent 裁决并标注。
- 硬约束（冻结，任何条款不得违反）：单文件 / 零依赖 / 无网络 / 无构建；`zombie_survival_save_v2` 与 `VER=2` 不动；新增状态字段一律先登记 `newState()`(L452) + `sanitizeSave()` 白名单，代码在后；不新增常驻 rAF 与全屏 filter/blur；`prefers-reduced-motion` 必须可降级。

## 一、Wave 1 · v2.1 范围锁定（≈21.3h，安全与测试占比 30.5%）
| 条款 | 内容 | 工时 | 状态 |
|---|---|---|---|
| C01 | 存档安全带：`.bak` 备份槽 + `sanitizeSave()` + `d.v` 版本闸门与迁移阶梯 + `render()` 崩溃护栏与 autosave 熔断 + 每次行动后自动存档 | 3.5h | 附修订通过（置顶冻结，不得后移或被小项插队） |
| C02 | 不变量断言脚本 + mulberry32 固定种子冒烟 | 3h | 通过（首跑若抓到既有违例，本轮只记录不修） |
| C03 | 首战降档 + 首战教学注入（`kills===0 && day<=1` → 敌数固定 1、排除尖叫者） | 0.5h | 通过 |
| C04 | 死前三次可操作提示（`S.flags.tips` 去重，≤18 字） | 1h | 通过 |
| C05 | 一次濒死救援 | 0.5h | 附修订通过（见 §二.1） |
| C06 | 新手引导层：常驻"下一步"行动条 + 6 条触发式微教学 | 2.5h | 通过（锁 Wave 1，不得再缩） |
| C07 | 护甲改百分比主导：`d = max(d*(1-min(.45,armor*.05)), d-armor*1.2, 1)`，兜底 `d*.4 → d*.55` | 0.5h | 通过（全场唯一零争议必修） |
| C08 | 拆除饥饿→感染死亡螺旋（消退阈值 45→25；归零掉血 6/8→4/5） | 0.3h | 附修订通过（见 §二.2） |
| C09 | 后期材料消耗出口：商人浮动汇率 `cost×(1+day*0.02)` + 复装追加 `tape×1` | 1h | 通过（商人均价须显式标注"今日汇率"） |
| C10 | 武器定位修正：步枪 `dmg26/ammo1/crit.30`，霰弹枪 `ammo:3`，精准维持 64/3 | 0.4h | 通过 |
| C11 | 防化服去绝对化 | 0.6h | 附修订通过（见 §二.3） |
| C12 | 昼夜色温层（四档，1.4s 过渡） | 1.5h | 通过（锁 Wave 1；仅 DOM 层 background-color/opacity，禁全屏 filter/backdrop-blur，RM 直切色） |
| C13 | 伤害数字锚定敌人卡/生命条 | 1h | 附修订通过（见 §二.4） |
| C14 | 移动端标签 4×2 全可见 + 主按钮热区 ≥44px | 0.5h | 通过（不再后移） |
| C18 | 夜间守夜战（尸潮可玩化） | 3.5h | 附修订通过（见 §二.5）——**由 Wave 2 上调进 Wave 1** |
| C24 | 数值微调包：深度搜索改正收益、陷阱按天数钝化、感染 60% "爆发期"可视化 | 1h | 通过（与 C07–C11 同批落地） |

**C17 预埋**：悬赏板整体后移 Wave 2 首位，但 v2.1 只预埋数据层（`S.ui.bounty` 过白名单、绑定 `S.stats.*`），验收现场须演示白名单键存在。

## 二、逐条修订（评审加锁，超出即视为未交付）
1. **C05**：整档仅一次 + 落盘不可重置 + 结果页写死"唯一救援已用" + **最终决战无效** + 仍扣一半材料 + 白名单前置；缺一即被玩家代表转为反对。
2. **C08**：与 C16 挂靠——C16 被砍则 C08 单条重议（不连坐 C07/C09/C10/C11）；交付时须给"螺旋非唯一压力源"数据。
3. **C11**：**三刀不许齐下**。必含替代收益（污染区搜刮加成 或 降低感染接触，二选一）；咬伤率 ≤.5；减伤倍率 0.6 **不动**（至多 0.65）；`staPenalty` 需换成收益或删除。
4. **C13**：必须包含敌人卡 100ms 一次性 `transform` 抖动（纯 CSS，无 rAF）；缺抖动 = C13 未交付。
5. **C18**：删掉"下调尸潮频率"半句，失败分支保留，威胁总量不降。
6. **C16（Wave 2）**：三项封顶——单精英 ≤1.5x、每日 ≤1 只、不与 flat 减免叠加；与 C18 绑定交付。
7. **C23（Wave 2）**：锁 Wave 2，不得挤占 C17 的位置，10h 不砍。
8. **X07 加锁**：C01 验收通过前，`SAVE_KEY` / `VER` / 存档结构一行不动。

## 三、Wave 2 · v2.2（顺序已裁决）
C17 悬赏委托板 → C16 精英词条（+C18 绑定交付）→ C20 区域与丧尸可辨识度（**允许缩为 3h，只做区域卡**）→ C21 支线人物线 → C22 装备改装与商人限购 → C15 战斗节拍器（带 `S.ui.pace` 快节奏开关，默认不开三段延迟）→ C19 环境底噪（音频并发上限必须随条落地并入 A9，做不到就砍 C19）→ C23 工程加固（IIFE 五分区 + 音频池 + 性能预算 p95<16ms）。
**X06 准入 gate**：C17 落地前须先提交"物资券不叠加材料通胀"算式，且奖励改为直接指向主线材料。

## 四、明确不做（全会通过，X02 坚守）
X01 材料耐久度 / **X02 区域枯竭与搜索次数上限（正式否决玩法组的 D2 储量方案，依据：收入已被 6 AP/天锁死 ≈11.2 材料/天）** / X03 框架·虚拟 DOM·TS 全量编译·拆文件打包 / X04 全屏 Canvas 粒子·常驻 rAF·外部字体音频图片 / X05 教程关卡与强制步骤锁 / X06 新增通用货币（附条件见上）/ X07 存档结构大改与 IndexedDB 迁移。

## 五、共享验收线（v2.1 交付必须全部提供证据）
1. `node --check` 通过 + C02 不变量脚本 PASS/FAIL 输出。
2. **C01 演练**：坏档注入（手工改坏 localStorage）+ `.bak` 回退 PASS；`.bak` 必须先写成功且回读校验通过，才允许覆写主键，失败则跳过本次写入。
3. **数值三钉子**（数值组提出，全会认可）：① 护甲 9 时第 45 天普通丧尸单次伤害 > 5；② 第 20 天材料净积累出现下行；③ 精英词条启用后第 ① 条仍成立。
4. 新手线：新档 3 分钟流程实测——首战 1 只、提示 ≥3 条、C13 抖动实测、移动端 375×812 截图过 OCR。
5. 老存档（当前 v2 档）加载不报错、字段不缺、进度不丢；新增字段白名单键现场演示。

## 六、未闭合项与主 agent 裁决
| 项 | 分歧 | 裁决 |
|---|---|---|
| C20 排位 | 白露要 Wave 2 锁位（可缩 3h） vs 季工要求排在数值组之后 | 采纳两者交集：锁 Wave 2，排在 C07–C11+C24 之后，允许缩为 3h 只做区域卡 |
| C21/C22 顺序 | 未终裁 | 排在 C17/C16 之后，按 §三 顺序执行 |
| X06 算式时点 | 未定 | 作为 C17 的准入 gate，不通过则 C17 顺延 |
| 整包最终表决 | 法定人数不足（1/5），未执行 | 按"附修订通过"执行；五方红线在第 2/3 轮均标为满足，故不阻塞开工。若要正式表决，另开一轮单议题投票 |

## 七、开工顺序（Wave 1 施工序）
1. C01 + C02（安全与可验证性，冻结置顶，6.5h / 占 30.5%）
2. C07 + C10 + C11 + C08 + C09 + C24（数值整组一次落地，避免中间态）
3. C03 + C04 + C05 + C06（新手链，依赖 C01 的字段白名单）
4. C13 + C12 + C14（表现与移动端，零状态字段）
5. C18（守夜战，最后做，因为它会改变夜晚结算路径，需在数值组稳定后接入）
