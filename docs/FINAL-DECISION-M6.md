# M6 最终决议（teams_negotiate room-mty82emo-3 · brief/3 轮，约 9 分钟）

状态：**条件共识（附修订通过）**，支持率 100%（5/5），法定人数达标。
审核对象：`IDEA-M6.md`（P 进度提速 / F 食物 / S 季节 / W 天气体温 / X 三个试玩 bug）。本文即第一波的实施基线。

## 一、两波范围（锁定）

**第一波（本轮已实现）**
- **X01~X03**：生命回写 clamp、菜园门槛下调、断粮提示给出路。
- **P01**：材料产出 普通 ×1.3 / 深搜 ×1.5；新增**拆解**（1 AP，每区块资源池，拆光为止）。
- **P02**：danger≥2 的军械类 POI **首次深搜必出一件装备**（按危险度分档，每 POI 一次）。
- **P03**：菜园 `木2+布1+罐头1`、工作台 `铁3+木2+胶带1`、净水器 `铁3+芯片2+胶带1`（各降一档）。
- **P05**：POI 面板列出"可能出什么/可能遇上谁"（掉落与敌人可见）。
- **F01**：林地/农田/郊区**采集**（1 AP，每区块次数上限 + 3 天再生，冬天≈0）。
- **F02**：真农业——地块数=菜园等级、播种、`生长天数 = 基准 ÷ 季节系数`、**两种作物**（蔬菜 5 天/2 份、麦子 9 天/5 份）、留种少收一茬、鲜菜 3~4 天腐坏、冬季户外停摆。
- **F03**：种子来自搜刮（农场/超市/商场/学校/营地）+ 采集偶得 + 营地购买。
- **F04（轻量）**：果干（野果×3+木×1）、腌菜（蔬菜×2+化工×1）——加工延长保质期。
- **取水（上移到第一波）**：雨/雪天自动积水（污水，**每天上限 2 份**）+ 煮沸（复用既有配方，耗燃料）。
- **S01/S02**：30 天一个季节（春 1-30/夏 31-60/秋 61-90/冬 91+）；作物系数 春1.0/夏0.8/秋1.3/冬0；腐坏 夏×1.5/冬×0.6；体温基线。
- **S03（最小）**：冬寒潮——采集≈0、户外作物停摆、体温加速流失。
- **W01/W02/W03（最小闭环）**：8 种天气按季节加权、每天掷一次、**可预报明天**；影响采集/作物/腐坏/火堆/移动/体温/取水；**体温 0~100（50 舒适），单阈值 + 一档 AP/命中惩罚，火堆或室内完全对抗，不做持续掉血**；HUD 加季节天气 + 体温两个 chip。

**第二波（下一轮）**：P04 材料包交易、F05 雨水收集器整包、S02 剩余（昼夜长度/丧尸活性）、S03 其余季节事件、W04 衣物保暖槽、W05 完整地图面板与环境详情。
**明确不做**：养殖、疾病传播链、灌溉管网、NPC 农业、四季贴图。

## 二、验收门槛（会议 C13/C16，硬指标）

| 指标 | 门槛 | 实测（40 局 × 100 天模拟） |
|---|---|---|
| 首设施（第一个据点设施） | 第 3~5 天 | **P50 第 3 天 / P90 第 3 天** ✅ |
| 首装备（首次深搜保底） | P50 ≤ 第 5 天 / P90 ≤ 第 8 天 | **P50 5 / P90 5** ✅ |
| 菜园首收 | P50 ≤ 第 10 天 / P90 ≤ 第 13 天 | **P50 8 / P90 9** ✅ |
| 冬季可再生食物（采集+菜园）÷ 消耗 | ≤ 70% | 冬季各行均 ≤ 0.7 ✅ |
| 每 AP 食物：采集 < 搜刮 | 必须成立 | 采集 0.09~0.1 vs 搜刮 0.33 ✅ |
| 第 30~100 天材料库存 | 不得单调上升 | 存在消耗日、末期有界 ✅ |
| 前 10 天三件套（红线） | 装备 + 可循环食物来源 + 已建成设施 | 模拟 + 探针双重验证 ✅ |

模拟脚本：`tests/sim100.test.ts`（与运行时**同源**读 `src/v4/env-core.ts`），输出 `docs/sim-100d.json`；不达标即测试变红（等于回滚）。

## 三、工程红线（会议 C14）

- 不改 `SAVE_KEY` 与 `VER=2`；新字段 `env`（天气今日/明日、体温、当日雨水）、`plots`（作物 id + 天数）进 `newState()` 与 `sanitizeSave` 白名单，旧档加载给默认值。
- 采集/拆解池挂在 `S.world.forage` / `S.world.salvage`（大世界存档里，随 visited 一起走）。
- 每波必须过 `tsc --noEmit` / `vitest` / `file://` 冒烟 / 两个浏览器探针。

## 四、第一波证据（详见 V40-ACCEPTANCE.md 第六节）

- 单测 **74/74**（新增 `tests/env.test.ts` 16 条 + `tests/sim100.test.ts` 9 条）。
- M6 探针：环境行、翻日掷天气且与预报一致、采集/拆解上限、菜园播种与收获、冬天拒绝播种、军械保底只给一次、`hp` clamp、断粮出路提示 —— 0 报错。
- 回归：M5 探针（最终决战两阶段/血月守夜）、三列布局（0 次渲染变更）全部照旧。
