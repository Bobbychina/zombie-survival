# 隐私与权限说明（zombie-survival / bobbychina.github.io/games）

这份文档说明这个游戏与游戏厅**会碰你哪些数据、存在哪里、怎么删掉**。项目没有后端、没有数据库、没有统计与埋点——所有代码都在这个仓库里，可以逐行核对。

---

## 1. 我们收集什么

**什么都不收集。** 没有服务器、没有账号数据库、没有访问日志、没有第三方统计。你的账号与存档从头到尾只存在于：

| 位置 | 存了什么 | 谁控制 |
|---|---|---|
| 你浏览器的 `localStorage` | 账号记录（用户名、邮箱（选填）、**加盐哈希后的口令**）、登录会话、本地存档、绑定的云令牌 | 你（清浏览器数据即消失） |
| 你自己的 GitHub **私有 Gist**（可选） | 存档 JSON 文件 + 一个 `manifest.json`（记录每个存档的更新时间） | 你（随时可以自己删） |
| 你自己的 OneDrive **应用文件夹** `/dsh-saves`（可选，当前未启用） | 同上 | 你 |

口令用 WebCrypto **PBKDF2-SHA256（210,000 次迭代 + 16 字节随机盐）** 派生，只存派生值与盐——不存明文口令，我们也无法还原它。

## 2. 绑定 GitHub 时申请的权限（授权页上看到的就是这两条）

申请范围写死在 `games/auth-config.js` 的 `scope: 'gist read:user'`。

| 权限 | 授权页上的说法 | 我们用它做什么 | 我们**不做**什么 |
|---|---|---|---|
| `gist` | Gists — Read and write access | 创建**一个私有 Gist**（描述为 `bobbychina.github.io/games 云存档（自动生成，可随时删除）`），在里面按 `<游戏>__<槽位>.json` 读写存档；换设备时先列出云端槽位再下拉 | 不读你的仓库、不读 issue/PR/Actions、不改你的代码、不发任何内容到别处 |
| `read:user` | Personal user data — Profile information (read-only) | **只在绑定那一刻**调一次 `GET /user`，取 `login` / `name` / `avatar_url` 用于界面显示（例如 `GitHub @bobbychina32747`） | 不读邮箱、不读关注列表、不写任何资料；此后同步存档不再调用该接口 |

代码里对 `api.github.com` 的访问只有两类：`/user` 与 `/gists*`（见 `src/account/account.js`，可用 `grep api.github.com` 自查）。

### 需要你知道的两个代价（GitHub 的粒度限制，不是我们能收窄的）

1. **`gist` 是全量授权**：GitHub 对 OAuth App 没有"只授权某一个 Gist"的粒度，所以拿到这个令牌的人理论上能读写你账号下的**所有** Gist（包括以前遗留的）。**但它碰不到你的仓库与代码。**
2. **令牌存在本机**：静态站没有后端，云令牌只能放在 `localStorage`。所以不要在这台电脑上把浏览器交给别人用，也别装来源不明的扩展——同源 XSS 或本地使用者都能读走这个令牌。

## 3. 你随时能收回（三条都很快）

| 想做什么 | 怎么做 |
|---|---|
| 撤销 GitHub 授权 | <https://github.com/settings/applications> → Authorized OAuth Apps → `bobbychina's games` → **Revoke**（云同步立刻失效，本地存档不受影响） |
| 只删云端存档 | 游戏厅 / 游戏账号面板里的 **删除**，或者直接在 GitHub 上删掉那个 Gist |
| 清掉本机一切 | 账号面板 → **🗑️ 注销账号**（删账号 + 它名下的本地存档）；或直接清空该站点的浏览器数据 |

## 4. 不想授权任何第三方账号

完全可以用：**本地账号 + 存档文件导入导出**。注册/登录照常，「💾 导出存档文件」下载一个 json，「📂 从文件导入」在另一台电脑还原。这条路径不涉及任何第三方服务。

## 5. 关于微软登录

当前**未启用**。启用需要 Azure 应用注册，而个人微软账号走注册流程时会被要求绑定信用卡，所以这条入口已从界面隐藏（代码保留在 `src/account/account.js`，填上 client ID 即可启用）。若启用，权限为 `openid profile offline_access User.Read Files.ReadWrite.AppFolder`：只读你的显示名与邮箱，以及**仅限应用自己的文件夹** `/dsh-saves` 的读写，无法访问你 OneDrive 里的其它文件。

## 6. 未成年人 / 校园环境

本项目是个人作品，不面向公众提供服务、不收集他人数据。如果你在学校设备上玩：账号与令牌会留在这台设备的浏览器里，用完请在账号面板注销并清理浏览器数据。

---

有任何一条与代码不符，欢迎提 issue 指出来——以代码为准。
